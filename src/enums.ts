export enum ButtonColor {
    Red = 'Red',
    Blue = 'Blue',
    Yellow = 'Yellow',
    Green = 'Green'
}

export enum Locales {
    en = 'en',
    es = 'es',
    fr = 'fr'
}

export enum Cookies {
    SOUND = 'sound',
    HIGH_SCORE = 'high_score',
    LOCALE = 'locale',
    HIGH_SCORE_DATE = 'high_score_date'
}

export enum KeyCode {
    SPACE = ' '
}
